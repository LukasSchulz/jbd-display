enum Pages {
  SoC,
  Battery_Voltages
};

enum Menus {
  pages,
  voltages
};

enum SoC_Menus {
  page_view,
  charge_state
};

enum Settings {
  normal_view,
  settings
};

enum SettingsOptions {
  screen_off_delay,
  device_id,
  can,
  victron_bms,
  back
};
